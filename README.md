# A resume built from JSON using Pug and SCSS

This is based off of the [Actual theme for JSON Resume](https://github.com/davcd/jsonresume-theme-actual).

## Export and serve resume

```bash
gulp --resume resume.json
```
